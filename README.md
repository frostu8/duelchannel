# `duelchannel`
The Duel Channel API.

## Getting an API token
To get an API token, run:

```sh
duelchannel register <server_name>
```

This will return an API token which can be used to authenticate with the server
through the `X-API-Key` header.
